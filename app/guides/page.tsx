import type { Metadata } from 'next'; import Link from 'next/link'; import { guides } from '@/data/guides';
export const metadata: Metadata = { title: '지원금 신청 가이드', description: '검색하기 쉬운 정부지원금 신청 방법과 자격 조건 안내입니다.' };
export default function GuidesPage() { return <main className="mx-auto max-w-5xl px-4 py-10"><p className="text-sm font-black text-brand">검색 유입용 정보글</p><h1 className="mt-2 text-4xl font-black">지원금 신청 가이드</h1><p className="mt-3 text-slate-600">자주 찾는 정책을 신청 대상·방법·준비사항 중심으로 정리했습니다.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{guides.map((guide) => <Link key={guide.slug} href={`/guides/${guide.slug}`} className="rounded-2xl border border-line bg-white p-6 transition hover:-translate-y-1 hover:shadow-lg"><span className="text-xs font-bold text-brand">{guide.category}</span><h2 className="mt-2 text-xl font-black">{guide.title}</h2><p className="mt-3 leading-6 text-slate-600">{guide.description}</p></Link>)}</div></main>; }
